# Honeypot Client Agent Environment Example
# Copy this file to client/.env and set API_KEY to match server/.env.

# Required: must match server/.env API_KEY.
API_KEY=change-this-to-match-server-api-key

# Optional default Server URL used when client_config.json does not exist.
# Normal deployments should still set server_url in client/client_config.json.
CLIENT_SERVER_URL=http://127.0.0.1:8000

# Optional Agent behavior tuning
AGENT_SYNC_INTERVAL=15
AGENT_REQUEST_TIMEOUT=15
AGENT_UPLOAD_BATCH_SIZE=200
AGENT_COLLECT_MAX_LINES_PER_FILE=5000

# Daemon log mode: errors writes stderr only, full writes stdout+stderr, off disables agent.log.
AGENT_DAEMON_LOG_MODE=errors
AGENT_DAEMON_LOG_MAX_BYTES=10485760
AGENT_DAEMON_LOG_BACKUP_COUNT=3

# Local SQLite log retention and cleanup interval
CLIENT_LOG_RETENTION_DAYS=30
CLIENT_LOG_CLEANUP_INTERVAL_SECONDS=3600

# Disk guard: when host disk usage reaches 80%, delete oldest local data until near 75%.
CLIENT_DISK_GUARD_ENABLED=true
CLIENT_DISK_USAGE_MAX_PERCENT=80
CLIENT_DISK_USAGE_TARGET_PERCENT=75
CLIENT_DISK_GUARD_PATH=/
CLIENT_DISK_GUARD_INTERVAL_SECONDS=300
CLIENT_DISK_GUARD_BATCH_ROWS=5000
CLIENT_DISK_GUARD_MAX_DB_BATCHES=10
CLIENT_DISK_GUARD_MIN_FILE_AGE_SECONDS=600
CLIENT_SQLITE_VACUUM_ON_DISK_GUARD=true

# Drop loopback/RFC1918/Docker bridge traffic before saving logs locally.
DROP_PRIVATE_IP_LOGS=true

# Optional custom whitelist file path
WHITELIST_PATH=whitelist.json
